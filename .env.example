# Rotiner v2 — variáveis de ambiente (só placeholders; NUNCA comitar valores reais)
# Copie para .env.local e preencha com o seu projeto Supabase.
# .env.local está no .gitignore (*.local). Só chaves VITE_* publishable — nunca service_role.

VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sua-publishable-anon-key-aqui
